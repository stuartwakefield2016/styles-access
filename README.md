# Stuart Wakefield 2016 Styles Access

Usage:

```sh
bower install --save stuartwakefield2016-styles-access
```

Add the class `access` to any element that you wish to only be visible to
screen readers.
